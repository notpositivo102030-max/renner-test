# Copie para .env fora do webroot ou configure no ambiente/secret manager.
# Mantido como referência; o código atual preserva credenciais legadas para compatibilidade.
ADMIN_USER=mafia
ADMIN_PASSWORD_HASH=
APP_ENV=production
APP_FORCE_HTTPS=true

# Gere com: php -r 'echo base64_encode(random_bytes(32)), PHP_EOL;'
APP_DATA_KEY=
